//pipeline {
    //agent { dockerfile true }
    //stages {
        //stage('Test') {
            //steps {
                //sh 'node --version'
                //sh 'svn --version'
            //}
        //}
    //}
//}

pipeline {
    agent {
        docker { image 'mcr.microsoft.com/dotnet/sdk:6.0-alpine' }
    }
    stages {
        stage('Test') {
            steps {
		sh 'ls'
            }
        }
    }
}